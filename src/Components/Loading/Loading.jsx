import './Loading.css';

export default function Loading() {
    return (
        // Loading spinner container for submitting data.
        <div className="spinner-container-submit">
            <div className="spinner"></div>
        </div>
    )
}